# ===================================
# Claw Staffer — Environment Config
# ===================================
# Copy this file to .env and fill in your values.

# --- Server ---
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# --- Auth ---
# Secret key for JWT tokens (generate a random string)
JWT_SECRET=change-me-to-a-random-secret-string
# Initial admin password (used on first boot to create admin account)
ADMIN_PASSWORD=admin123

# --- Database ---
# Path to SQLite database file (created automatically)
DATABASE_PATH=./data/autostaff.db

# --- AI Providers (set the ones you want to use) ---
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_AI_API_KEY=

# --- Ollama (local models) ---
XAI_API_KEY=xai-...
OLLAMA_BASE_URL=http://localhost:11434

# --- Telegram ---
# Default bot token (users can also add tokens via the web UI)
TELEGRAM_BOT_TOKEN=

# --- Webhook (for Railway/production) ---
# Public URL where this server is accessible (e.g. https://your-app.up.railway.app)
# Required for Telegram webhook mode. Leave empty for polling mode.
PUBLIC_URL=

# --- Logging ---
# Log level: silly, trace, debug, info, warn, error, fatal
LOG_LEVEL=info
